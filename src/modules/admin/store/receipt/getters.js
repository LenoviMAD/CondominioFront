export function getCurrentItem(state) {
        return state.currentItem;
}

export function getItems(state) {
        return state.items;
}

export function getVouchers(state) {
        return state.vouchers;
}

export function getDivision(state) {
        return state.division;
}

export function getFavorite(state) {
        return state.getFavorite;
}
