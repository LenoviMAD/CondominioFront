export function isModalOpen(state) {
        return state.isModalOpen;
}

export function getCurrentItem(state) {
        return state.currentItem;
}

export function getPlan(state) {
        return state.plan;
}

export function getTimePlan(state) {
        return state.timePlan;
}
